# No secrets required for NPM itself.
# Cloudflare DNS challenge credentials are configured inside the NPM admin UI
# at http://127.0.0.1:81 (access via SSH tunnel: ssh -L 81:localhost:81 user@fleming)
