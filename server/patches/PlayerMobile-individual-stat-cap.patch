--- a/Projects/UOContent/Mobiles/PlayerMobile.cs
+++ b/Projects/UOContent/Mobiles/PlayerMobile.cs
@@ -591,7 +591,7 @@
             {
                 if (Core.ML && AccessLevel == AccessLevel.Player)
                 {
-                    return Math.Min(base.Str, 150);
+                    return Math.Min(base.Str, ClusterFStatCaps.IndividualStatCap);
                 }

                 return base.Str;
@@ -606,7 +606,7 @@
             {
                 if (Core.ML && AccessLevel == AccessLevel.Player)
                 {
-                    return Math.Min(base.Int, 150);
+                    return Math.Min(base.Int, ClusterFStatCaps.IndividualStatCap);
                 }

                 return base.Int;
@@ -621,7 +621,7 @@
             {
                 if (Core.ML && AccessLevel == AccessLevel.Player)
                 {
-                    return Math.Min(base.Dex, 150);
+                    return Math.Min(base.Dex, ClusterFStatCaps.IndividualStatCap);
                 }

                 return base.Dex;
